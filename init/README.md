# Init Folder

This folder contains initialization files used to set up the database.

Example:
- data.js → Used to insert initial data into the database.
- index.js → This folder contains the database initialization script that connects to MongoDB 
             and seeds the database with sample listing data.